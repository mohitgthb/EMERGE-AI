# CNN Verifier Dependencies
# Add these to your existing requirements.txt

# Core ML dependencies (if not already present)
torch>=2.0.0  # PyTorch for CNN verification
torchvision>=0.15.0  # For image transforms
numpy>=1.21.0
opencv-python>=4.7.0

# Already required by YOLO (should be present):
# ultralytics>=8.0.0
